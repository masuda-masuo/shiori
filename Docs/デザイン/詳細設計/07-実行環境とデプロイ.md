# 詳細設計: 実行環境とデプロイ

## 目的

ホストを汚さず、再現性のある形で DB と MCP サーバーを動かす。

## 構成: Docker Compose（2 サービス）

- `db`: PostgreSQL（pgvector + pgroonga/pg_bigm 入りイメージ）。
- `app`: MCP サーバー（自作コード）。
- **分離する理由:** app を再ビルド／再起動しても DB を巻き込まない（索引の再構築回避）、コンテナの 1 プロセス原則（PID1）に沿う、DB は公式系イメージを活用。

## 永続化

- DB のデータディレクトリは **named volume** に置く（コンテナの書き込みレイヤーに置くと再ビルドで消え、埋め込みを作り直す羽目になる）。
- 埋め込みモデルの重みも volume にキャッシュ。

## 起動

- `docker compose up` で db + app が立ち上がる。

## 検討事項 / 未決

- ingest（同期）をどう動かすか: app 内のジョブ / 一回限りのコンテナ実行 / スケジュール。
- 非公開リポジトリ用のトークンの受け渡し（環境変数 / シークレット）。
